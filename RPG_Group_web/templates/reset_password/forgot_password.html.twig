{% extends 'base.html.twig' %}

{% block title %}Tower Of Destiny — Forgot Password{% endblock %}

{% block stylesheets %}
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Cinzel:wght@400;600;900&display=swap"
    rel="stylesheet">
<link rel="stylesheet" href="{{ asset('css/registre.css') }}">
{% endblock %}

{% block body %}

<!-- Particules flottantes -->
<div class="particles" aria-hidden="true">
    <div class="particle" style="--x:3%;  --dur:9s;  --delay:0s;"></div>
    <div class="particle" style="--x:10%; --dur:12s; --delay:1.5s;"></div>
    <div class="particle" style="--x:18%; --dur:8s;  --delay:3s;"></div>
    <div class="particle" style="--x:27%; --dur:14s; --delay:0.5s;"></div>
    <div class="particle" style="--x:38%; --dur:7s;  --delay:4s;"></div>
    <div class="particle" style="--x:50%; --dur:11s; --delay:2s;"></div>
    <div class="particle" style="--x:61%; --dur:9s;  --delay:1s;"></div>
    <div class="particle" style="--x:72%; --dur:13s; --delay:5s;"></div>
    <div class="particle" style="--x:83%; --dur:8s;  --delay:2.5s;"></div>
    <div class="particle" style="--x:94%; --dur:10s; --delay:3.5s;"></div>
</div>

<div class="register-card">

    <!-- Logo -->
    <div class="register-logo">
        <span class="shield-icon">🔐</span>
        <h1>Forgot<br>Password</h1>
        <p class="tagline">Recover your destiny, hero</p>
    </div>

    <div class="divider"><span>✦</span></div>

    <!-- Messages Symfony -->
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert-rpg alert-danger">{{ flash_error }}</div>
    {% endfor %}

    <div class="alert-rpg alert-info">
        Enter your email and we will send you a magic link to reset your password.
    </div>

    <!-- Formulaire -->
    {{ form_start(forgotPasswordForm, {'attr': {'class': 'register-form'}}) }}

    <!-- Email -->
    <div class="form-group">
        <label for="forgot_password_form_email">📧 Email</label>
        <div class="input-wrap">
            <span class="input-icon">✉</span>
            {{ form_widget(forgotPasswordForm.email, {
                'attr': {
                    'placeholder': 'hero@destiny.com',
                    'autocomplete': 'email',
                    'class': 'rpg-input'
                }
            }) }}
            <div class="focus-bar"></div>
        </div>
        <div class="field-error">
            {{ form_errors(forgotPasswordForm.email) }}
        </div>
    </div>

    <button class="btn-register" type="submit">
        🛡 Send Reset Link
    </button>

    {{ form_end(forgotPasswordForm) }}

    <!-- Footer -->
    <div class="register-footer">
        <a href="{{ path('app_login') }}">← Back to login</a>
        <span class="footer-sep">✦</span>
        <a href="{{ path('app_register') }}">Create account</a>
        <span class="footer-sep">✦</span>
        <a href="{{ path('app_rpg') }}">← Back to tower</a>
    </div>

</div>

{% endblock %}